# AGENTS

## 作用域

本文件作用于仓库根目录及其全部子目录；若下层目录新增 `AGENTS.md`，以更近一层覆盖冲突规则。

## 规则优先级

只有落盘到 `AGENTS.md` 的规则才视为仓库协作规则。README、代码注释、对话中的临时偏好或未落盘说明，不构成稳定约束。

## 变更边界

1. 先做最小盘点，再开始改动。
2. 只修改与当前任务直接相关的仓库内文件。
3. 不因目录存在就默认新增模块、执行入口、注册关系或 workspace 边界。
4. 不回滚、覆盖或清理自己未引入的现有改动，除非得到明确确认。

## 暂停条件

1. 需求边界不清且存在多种互斥实现时，先暂停并请求确认。
2. 需要新增模块、入口、注册关系、workspace 边界或执行面模式时，先暂停并请求确认。
3. 发现仓库结构或控制面职责异常，且无法在当前任务内做最小修复时，先说明问题并暂停扩展修改。

## 人工接管条件

1. 需要执行破坏性操作、历史改写或覆盖用户现有改动。
2. 无法完成最小相关验证，且继续提交会掩盖真实风险。

## 文件边界

1. `AGENTS.md` 只承载协作约束。
2. `README.md` 只承载项目概述、职责边界和阅读入口。
3. `justfile` 只承载稳定、可重复执行的命令入口。

## 基本工程约束

1. 能做最小相关验证时，先验证再交付；无法验证时明确说明缺口、原因与风险。
2. 优先保持现有分层与 API 边界，不为局部便利做无关重构。
3. 文档、注释、错误和日志信息尽量使用中文，专用名称除外。
